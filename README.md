# my-chatbot

A simple web chatbot that connects to your own API (OpenRouter or a custom endpoint).

## Run locally
```
npm install
npm run dev
```

## Deploy
Push this folder to a GitHub repo, then import it in Vercel (vercel.com) — it auto-detects Vite and deploys.
